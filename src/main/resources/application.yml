app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/patient-db
    username: postgres
    password: password
    pool-size: 30

spring:
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl